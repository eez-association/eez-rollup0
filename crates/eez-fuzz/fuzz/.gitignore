target/
artifacts/
coverage/
# corpus/ IS tracked — the committed cases are both the fuzzer seed corpus AND
# the CI regression set (replayed by tests/corpus_replay.rs). `fuzz.sh run`
# extends it (new coverage → new files); `fuzz.sh cmin` + commit to curate.
# The root workspace Cargo.lock is authoritative (this is a workspace member).
Cargo.lock
