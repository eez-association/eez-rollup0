target/
artifacts/
coverage/
# The live campaign corpus is ignored, but the committed seed files under
# corpus/ are tracked (already in the index) and ARE the CI regression set
# (replayed by tests/corpus_replay.rs). Curate with `cargo fuzz cmin` + commit.
corpus/
# The root workspace Cargo.lock is authoritative (this is a workspace member).
Cargo.lock
