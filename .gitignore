# Rust
target/

# Foundry
contracts/out/
contracts/cache/
contracts/broadcast/

# Local config (contains private keys) + machine-written deploy outputs
.env
deployments.env

# Default L2 datadir if it lands in the repo
datadir/

# Kurtosis devnet local config (may contain keys)
infra/kurtosis/.env
infra/kurtosis/endpoints.env
infra/kurtosis/args.yaml

data/
chiado-*/

# CI diagnostics.
artifacts/
