# Rust
target/

# Foundry
contracts/out/
contracts/cache/
contracts/broadcast/

# Local config (contains private keys) + machine-written deploy outputs
.env
deployments.env

# Default L2 datadir if it lands in the repo
datadir/

chiado-db*/
data/
# Chiado run secrets (funded keys)
.env.chiado

# Downloaded gnosis chiado consensus config (README one-time setup)
configs/chiado/
.cargo/config.toml

# 2nd-composer + backup env files (contain private keys — never commit)
.env.chiado-2
.env.*.bak-*
*.bak-*
deployments.env.*-backup-*
